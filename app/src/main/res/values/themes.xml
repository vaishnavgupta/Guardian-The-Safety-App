<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GuardianSafetyApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="rippleColor">@color/graydarkdisabled</item>
        <item name="android:selectableItemBackground">@color/graydarkdisabled</item>
    </style>

    <style name="Theme.GuardianSafetyApp" parent="Base.Theme.GuardianSafetyApp" />

</resources>